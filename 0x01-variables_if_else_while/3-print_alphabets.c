#include <stdio.h>
/**
 * main - Entry point
 * 
 * Return: returns 0
*/

int main(void)
{
    char a;

    for (a = 97; a < 123; a ++)
    {
        putchar(a);
    }

    for (a = 65; a < 91; a++)
    {
        putchar(a);
    }
    return (0);
}
